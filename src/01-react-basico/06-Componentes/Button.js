import React from 'react';

const Button = () => {
  const buttonStyleCSS = {backgroundColor: 'green', color: '#0E0', border: '2px solid #0E0'};
  return <button style={ buttonStyleCSS }>Enviar</button>;
};

export default Button;
